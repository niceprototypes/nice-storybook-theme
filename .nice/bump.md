[2026-09-15 20:10] minor: Initial extraction — token-styled sidebar tree (tagSidebarPaths engine + glyph restyle + manager-sidebar CSS + theme sync + hidden search), moved out of nice-storybook into a standalone manager-only addon. The preview-side StoryNavigation bar is intentionally out of scope.
[2026-09-16 12:00] minor: Pull the manager chrome reset + background stylesheets (manager-reset.css, manager-background.css) into the addon, injected before the sidebar sheet in cascade order.
[2026-09-19 07:58] minor: Ports the sidebar depth-awareness, the close-menu glyph swap and the breakpoint-derived rules, and stops reaching outside its own surface: the global [data-state=visible] rule is scoped to the sidebar's scroll area (it silenced any Radix component another addon renders), addons.setConfig no longer races the consumer's own call, and the body colours come from tokens rather than literals. Also fixes the tsup external so storybook subpaths are not bundled.
[2026-09-24 18:30] minor: Rebuild the sidebar tree for any nesting depth. The engine now injects a guide strip into each row (one cell per depth level, tagged data-branch / data-path) instead of stamping depth-specific row attributes, so indentation and connector lines come from the cells and the stylesheet has no per-depth rules; data-path-depth, data-branch-end, data-before-selected, data-selected-group-item and data-selected-depth are removed. Connectors are token-coloured CSS gradients (the branch glyph SVGs are gone), selection ancestry follows data-parent-id instead of an id-prefix test, and the stylesheet reads tokens through getToken.
